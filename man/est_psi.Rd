% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/est_s_t_y.R
\name{est_psi}
\alias{est_psi}
\title{One-step, split sample estimator for E[Y(t)], E[Y(t)|R=1], E[Y(t)|R=0],
and E[Y(t)-Y0], E[Y(t)-Y0|R=1], E[Y(t)-Y0|R=0]

Estimates study-specific and overall outcome means (and difference from baseline)
using cross-fitting with single index models (SIMs) and
nuisance models for treatment and outcome missingness (\code{mgcv::gam}). The function
also computes influence-function-based variances, confidence intervals, and
optional truncated influence-function diagnostics.}
\usage{
est_psi(
  Y,
  M,
  Y0 = NULL,
  R,
  X,
  t,
  trt,
  gamma,
  fold,
  seed,
  IF_output,
  simple_trunc,
  quant,
  kernel,
  single_index_method,
  method = "optim",
  use_mave = TRUE,
  s_t_y = NULL,
  coef_g.fit = NULL,
  coef_t_R0.fit = NULL,
  coef_t_R1.fit = NULL,
  coef_M_R0.fit = NULL,
  coef_M_R1.fit = NULL
)
}
\arguments{
\item{Y}{Numeric outcome vector. Missing values are internally replaced with
\code{0} prior to model fitting.}

\item{M}{Binary indicator for observed outcome (\code{1} = observed, \code{0} =
missing).}

\item{Y0}{Numeric baseline outcome vector. No missing data.}

\item{R}{Binary group indicator used to stratify nuisance and outcome models.}

\item{X}{Data frame or matrix of baseline covariates.}

\item{t}{Treatment assignment vector.}

\item{trt}{Treatment level for which the target estimand is computed.}

\item{gamma}{Numeric vector of sensitivity parameters.}

\item{fold}{Number of cross-fitting folds.}

\item{seed}{Optional integer random seed for fold assignment. Use \code{NULL} to
leave RNG state unchanged.}

\item{IF_output}{Logical; if \code{TRUE}, include influence-function vectors in
the returned list.}

\item{simple_trunc}{Logical; if \code{TRUE}, apply quantile truncation to inverse
probability weights. If \code{FALSE}, apply IF truncation diagnostics.}

\item{quant}{Numeric in \verb{(0, 1)} used as the upper quantile for simple weight
truncation when \code{simple_trunc = TRUE}.}

\item{kernel}{Characters; Kernel used for SIMs. \code{K2_Biweight} for Epanechnikov kernel,
\code{dnorm} for Gaussian kernel.}

\item{single_index_method}{Characters; Three implementations for SIMs: \code{fixed_bandwidth}
for setting bandwidth to 1, \code{fixed_coef} for setting the first coefficient to 1, and \code{norm1coef}
for setting the norm of coefficients to 1.}

\item{method}{Characters; Optimization method used for SIMs. Choices are: \code{optim}, \code{nlminb}, \code{nmk}.
Note that method is set to \code{optim} if single_index_method=\code{norm1coef}.}

\item{use_mave}{Logical; if \code{TRUE}, use Minimum Average Variance Estimation (MAVE) method for initial
coefficients value for SIMs. If \code{FALSE}, use sliced inverse regression. Default is \code{TRUE}.}

\item{s_t_y}{A function of Y in the exponential tilting model. If NULL, s_t_y is set to pnorm((y-60)/25).}

\item{coef_g.fit}{Optional starting values for a treatment model; currently
retained for interface compatibility.}

\item{coef_t_R0.fit}{Optional starting coefficients for treatment model fit
in \code{t=trt} and \code{R = 0}stratum.}

\item{coef_t_R1.fit}{Optional starting coefficients for treatment model fit
in \code{t=trt} and \code{R = 1} stratum.}

\item{coef_M_R0.fit}{Optional starting coefficients for missingness model fit
in \code{R = 0} stratum.}

\item{coef_M_R1.fit}{Optional starting coefficients for missingness model fit
in \code{R = 1} stratum.}
}
\value{
A named list of estimates and uncertainty summaries for each value in
\code{gamma}. Core elements include point estimates (\code{est}, \code{est_R1}, \code{est_R0}), variance
estimates (\code{var}, \code{var_R1}, \code{var_R0}), and confidence interval bounds (\verb{lowerCI*}, \verb{upperCI*}).
Additional components depend on \code{simple_trunc} and \code{IF_output}:
\itemize{
\item \code{simple_trunc = TRUE}: returns quantile-weight-truncated summaries only.
\item \code{simple_trunc = FALSE}: additionally returns truncated summaries and
truncated IF objects when requested.
\item \code{IF_output = TRUE}: includes influence-function lists (\verb{IF*}) and,
when relevant, truncated IF lists (\verb{IF_trunc*}).
}
}
\description{
One-step, split sample estimator for E[Y(t)], E[Y(t)|R=1], E[Y(t)|R=0],
and E[Y(t)-Y0], E[Y(t)-Y0|R=1], E[Y(t)-Y0|R=0]

Estimates study-specific and overall outcome means (and difference from baseline)
using cross-fitting with single index models (SIMs) and
nuisance models for treatment and outcome missingness (\code{mgcv::gam}). The function
also computes influence-function-based variances, confidence intervals, and
optional truncated influence-function diagnostics.
}
\details{
The procedure uses sample-splitting/cross-fitting to reduce overfitting bias
in nuisance estimation. Outcome regressions are fit with single index models
and then integrated over estimated conditional distributions to obtain
conditional means and sensitivity-adjusted moments.
}
\examples{
# out <- est_psi(Y, M, Y0, R, X, t, trt = 1, gamma = c(0, 0.5),
#                fold = 5, seed = 1, IF_output = FALSE,
#                simple_trunc = TRUE, quant = 0.99, kernel="dnorm", 
#                single_index_method="norm1coef", method="optim")
}
